#include <bits/stdc++.h> 
void rotate(vector<int>& arr, int n) {
    int k = 1;
    k = k % n;
    vector<int>ans(n);
    for(int i=0; i<n; i++){
        ans[(i + k) % n] = arr[i];
    }
    arr = ans;
}
