#include <bits/stdc++.h> 
string convertString(string str) 
{
	int n= str.size();
	for(int i=0; i<n; i++){
		str[0] = toupper(str[0]);
		if(str[i] == ' '){
			str[i+1] = toupper(str[i+1]);
		}
	}
	return str;
}
